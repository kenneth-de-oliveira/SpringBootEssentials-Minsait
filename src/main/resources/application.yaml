spring:
  application:
    name: SpringBootEssentials - Minsait
  datasource:
    driver-class-name: org.h2.Driver
    password: sa
    url: jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1
    username: sa

server:
  port: 8080
